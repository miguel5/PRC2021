extends layout 

block content 

    header.w3-margin-left 
        h1.w3-text-orange Repositório #{repos} -
            span Classes 

    if classes.length 
        each c in classes 
            ul
                li
                    a(href=`/repos/${repos}/classe/${c}`)
                        b= c

    else
        p.w3-margin-left Não existem classes neste repositório.

    a(href="/").w3-margin-left Página inicial